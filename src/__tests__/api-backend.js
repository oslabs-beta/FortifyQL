// import request from 'supertest';
// import server from '../../server/server';

test('should return empty array when no test are selected', async () => {
//   const payload = {
//     API: 'https://swapi-graphql.netlify.app/.netlify/functions/index',
//     tests: [],
//   };

//   const response = await request(server).post('/api/runpentest').send(payload);
//   expect(response.body).toEqual([]);
});

test('should return status code 200 when tests are submitted', async () => {
//     const payload = {
//       API: 'https://example.com/graphql',
//       tests: ['Batching', 'Circular'],
//     };
  
//     const response = await request(server).post('/api/runpentest').send(payload);
//     expect(response.statusCode).toBe(200);
  });
